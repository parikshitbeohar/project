# Copy this file to .env.local (already gitignored via the *.local rule)
# and fill in real values there. Never commit .env.local.

# Only for a "publishable"/domain-restricted client key — see the comment
# above getWeatherApiKey() in src/config/apiConfig.ts for why a secret
# server-side key must NOT go here.
VITE_WEATHER_API_KEY=
